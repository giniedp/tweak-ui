# Poll

Polls can be used to display frequently changing values without requiring user interaction and without running mithril's redraw cycle. This is useful for things like FPS counters, Memory usage etc.

<Example/>

<ExampleCode/>
