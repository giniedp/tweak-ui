# Theme

Tweak ui comes with a default theme and the alternatives `.twui-dark` and `.twui-light`.
Add one of these classes to the root element to change the look.

## Theme variables

<Example name="example-theme.ts"/>
